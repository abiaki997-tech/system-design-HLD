{
  "name": "jdbc-sink-connector",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "processed-orders",
    "connection.url": "jdbc:mysql://localhost:3306/kafkadb",
    "connection.user": "root",
    "connection.password": "Welcome@1234",
    "auto.create": "true",
    "insert.mode": "insert",
    "pk.mode": "record_key",
    "pk.fields": "id",
    "table.name.format": "processed_orders"
  }
}